import { SignalsBacktestsModule } from './signals-backtests.module';

describe('SignalsBacktestsModule', () => {
  it('is defined', () => {
    expect(SignalsBacktestsModule).toBeDefined();
  });
});
